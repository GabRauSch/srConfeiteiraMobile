import { Text, View } from "react-native";
import { styles } from "../styles/screen.Products";

export const ProductItem = () => {
    return (
        <View style={styles.page}>
            <Text>Quem clicou aqui é feia e barriguda</Text>
        </View>
    );
};
