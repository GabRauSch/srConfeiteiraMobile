import React from 'react';
import { styles } from '../styles/component.HorizontalLine';
import { View } from 'react-native';

export const HorizontalLine = () => {
    return (
        <View style={styles.horizontalLine}></View>
    );
};
